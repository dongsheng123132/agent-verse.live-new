export const metadata = {
  title: 'AgentGrid.OS v3',
  description: 'AI Agent-driven Grid World Marketplace',
};

export default function GridV3Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}
